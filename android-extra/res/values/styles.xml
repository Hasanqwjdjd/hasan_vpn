<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- تمی که Activity را کاملاً نامرئی می‌کند (بدون پنجره).
         Theme.Translucent.NoDisplay در public SDK وجود ندارد،
         پس این معادل را می‌سازیم. -->
    <style name="Theme.HasanVPN.NoDisplay" parent="@android:style/Theme.Translucent.NoTitleBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowDisablePreview">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>
</resources>
