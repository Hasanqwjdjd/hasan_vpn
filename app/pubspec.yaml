name: hasan_vpn
description: Hasan VPN
publish_to: 'none'
version: 3.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  url_launcher: ^6.2.4

dev_dependencies:
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
