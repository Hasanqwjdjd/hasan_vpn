name: hasan_vpn
description: Hasan VPN
publish_to: 'none'
version: 3.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_vless: ^1.1.5

dev_dependencies:
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
