name: hasan_vpn
description: Hasan VPN
publish_to: "none"
version: 10.0.0+100

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_vless: ^1.1.5
  http: ^1.1.0
  shared_preferences: ^2.2.2
  url_launcher: ^6.2.4
  package_info_plus: ^8.0.0

dev_dependencies:
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
